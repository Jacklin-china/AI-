# KANTOKU 视觉设计规范

版本：1.2（桌面端）

适用范围：监督酱全部桌面端页面。新增或重构页面前必须先阅读本文件。

## 1. 设计原则

- 参考 Codex 的桌面工作区结构：稳定侧栏、轻量顶栏、居中的任务内容和直接的操作反馈。
- 视觉关键词：简约、安静、中性、清晰、可信。
- 内容和作品是视觉中心，界面本身不得抢夺注意力。
- 禁止点阵背景、装饰光晕、聚光卡片、荧光大标题、玻璃拟态及无意义入场动画。
- 当前版本只设计桌面端，页面最小宽度为 `1120px`。

## 2. 配色

| Token | 色值 | 用途 |
| --- | --- | --- |
| `--bg` | `#0F0F0F` | 页面背景、顶部栏 |
| `--sidebar` | `#171717` | 左侧导航 |
| `--surface` | `#1E1E1E` | 面板、卡片、弹窗 |
| `--surface-hover` | `#252525` | 悬停和选中背景 |
| `--surface-input` | `#151515` | 输入框、画布空状态 |
| `--line` | `#303030` | 默认边框和分隔线 |
| `--line-strong` | `#454545` | 强调边框 |
| `--text` | `#ECECEC` | 标题和主要文字 |
| `--text-secondary` | `#B4B4B4` | 正文和字段值 |
| `--muted` | `#8B8B8B` | 说明、标签、时间 |
| `--disabled` | `#5F5F5F` | 禁用和弱信息 |
| `--success` | `#63C99D` | 成功、在线 |
| `--warning` | `#E0B45B` | 处理中、待确认 |
| `--danger` | `#EF7373` | 错误、失败 |

主操作使用 `#F2F2F2` 白色背景和 `#161616` 深色文字。除状态信息外，不使用彩色品牌大面积强调。

## 3. 字体

字体栈：

```css
font-family: Inter, ui-sans-serif, -apple-system, BlinkMacSystemFont,
  "Segoe UI", "Microsoft YaHei UI", sans-serif;
font-family: "Cascadia Mono", "SFMono-Regular", Consolas, monospace;
```

| 层级 | 字号 | 字重 | 行高 | 用途 |
| --- | --- | --- | --- | --- |
| 页面标题 | `28px` | 600 | 1.25 | 页面主要任务 |
| 模块标题 | `16px` | 600 | 1.35 | 表单、画布、审片模块 |
| 卡片标题 | `15px` | 600 | 1.4 | 小型信息卡 |
| 正文 | `13px` | 400 | 1.6 | 描述和输入值 |
| 辅助文字 | `11–12px` | 400 | 1.55 | 状态、提示、按钮 |
| 元数据 | `9–10px` | 400 | 1.4 | 模型名、编号、英文标签 |

- 中文正文不得小于 `11px`。
- 请求 ID、金额、模型名和画面编号使用等宽字体。
- 页面内最多出现三级字号，避免宣传页式巨大标题。

## 4. 间距

使用 `4px` 基础栅格：`4 / 8 / 12 / 16 / 20 / 24 / 32`。

- 主内容最大宽度：`1500px`；水平内边距：`32px`。
- 页面标题区下边距：`24px`。
- 主工作区列间距：`16px`。
- 普通面板内边距：`16–20px`。
- 表单字段间距：`12px`；标签与控件间距：`6px`。
- 按钮组间距：`8px`。
- 输入控件高度约 `40px`；主按钮高度 `44px`。

## 5. 组件样式

### 按钮

- 主按钮：白底深色字，`8px` 圆角，禁止常驻阴影和位移动画。
- 次按钮：透明或输入区背景，`1px solid #454545` 边框，`8px` 圆角。
- 悬停只改变背景或文字颜色；链接按钮使用下划线反馈。
- 聚焦环：`2px solid #D9D9D9`，不得仅靠颜色变化表达焦点。

### 卡片和面板

- 背景 `#1E1E1E`，边框 `1px solid #303030`，圆角 `10px`。
- 默认无阴影；只有浮动通知和弹窗允许使用低强度阴影。
- 禁止 Spotlight、发光边框、渐变描边和悬停上浮。

### 输入控件

- 背景 `#151515`，边框 `#303030`，圆角 `8px`。
- 内边距 `10px 12px`，字号 `13px`。
- 聚焦边框 `#707070`，错误说明同时使用颜色和文字。

### 导航栏

- 桌面侧栏宽 `252px`，背景 `#171717`，右侧使用 `#252525` 分隔线。
- 导航项高度 `40px`，图标 `18px`，圆角 `8px`。
- 当前项使用 `#2A2A2A` 背景，不使用亮色指示条。
- 顶栏高度 `56px`，只显示当前工作区和系统状态。
- 导航按“工作区 / 制作工具”分组；需求、提示词、生成、审片、任务记录不得堆在同一面板。
- 侧栏底部固定展示当前项目总预算、已消耗、预占中和可用余额；金额使用等宽字体。

### 状态与弹窗

- 状态点直径 `7px`；状态文字必须与颜色同时出现。
- 弹窗宽度上限 `460px`，圆角 `12px`，操作按钮靠右排列。
- 任何付费操作必须在弹窗中清楚显示费用上界后才能确认。

## 6. 布局

- 页面结构固定为：`252px 侧栏 + 56px 顶栏 + 居中主内容区`。
- 创作页标题区保持紧凑，其后是可点击的四步流程线和 `410px + 自适应` 双栏工作区。
- 左栏一次只显示一个当前工具；右栏始终保留实时画面框，切换工具不得让画面消失。
- 打开页面时允许从本机任务记录恢复最近已有画面，但不得因此调用模型或创建新任务。
- 历史记录放入“任务记录”工具；档案页使用三列网格。
- 页面不为手机端重排；桌面窗口小于最小宽度时允许浏览器水平滚动。

## 7. 交互

- 页面打开和切换视图不使用入场动画。
- 悬停反馈时长 `150ms`，不移动组件位置。
- 运行中状态只允许低频透明度变化。
- 打开页面不得自动调用模型；生成、视觉质检和账单操作必须由用户明确触发。
