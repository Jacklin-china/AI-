# KANTOKU 视觉设计规范

版本：1.0
适用范围：监督酱全部桌面端与浏览器端页面。新增或重构页面前必须先阅读本文件。

## 1. 设计原则

- 产品定位是专业创作控制台，界面服务于作品判断，不与作品争夺注意力。
- 视觉关键词：冷静、精密、克制、可信、轻科技感。
- 参考 React Bits 的 Spotlight Card、Dot Grid、Line Sidebar、Animated List；只使用低强度反馈。
- 禁止大面积炫彩渐变、持续粒子雨、夸张玻璃拟态、巨大圆角、无意义漂浮动画和模板化 AI 紫色。
- 动效必须支持 `prefers-reduced-motion`，不得阻塞操作或改变布局。

## 2. 配色

### 基础色

| Token | 色值 | 用途 |
| --- | --- | --- |
| `--bg` | `#090B0F` | 页面最底层背景 |
| `--bg-elevated` | `#0D1117` | 顶栏、侧栏与悬浮区域 |
| `--surface` | `#11161D` | 主要卡片 |
| `--surface-raised` | `#171D25` | 悬停、二级面板 |
| `--surface-input` | `#0C1016` | 输入框、画布空状态 |
| `--line` | `#27303A` | 默认边框 |
| `--line-strong` | `#3B4652` | 聚焦与高层级分隔线 |

### 品牌与状态色

| Token | 色值 | 用途 |
| --- | --- | --- |
| `--accent` | `#D7FF55` | 主操作、当前步骤、关键数字 |
| `--accent-soft` | `rgba(215, 255, 85, 0.12)` | 品牌色低强度背景 |
| `--cyan` | `#62E6D3` | 成功、在线状态、辅助信息 |
| `--warning` | `#F4C96B` | 未知账单、待确认 |
| `--danger` | `#FF746C` | 错误、失败、危险操作 |

### 文字色

| Token | 色值 | 用途 |
| --- | --- | --- |
| `--text` | `#F2F5F7` | 标题与重要正文 |
| `--text-secondary` | `#B2BBC5` | 普通正文、字段值 |
| `--muted` | `#7F8995` | 标签、说明、时间 |
| `--disabled` | `#505A65` | 禁用状态 |

正文与背景对比必须清晰。品牌色只用于 10% 以内的视觉面积，禁止整页铺满。

## 3. 字体

字体栈：

```css
font-family: "Segoe UI Variable", "Microsoft YaHei UI", "PingFang SC", sans-serif;
font-family: "Cascadia Mono", "SFMono-Regular", Consolas, monospace;
```

| 层级 | 字号 | 字重 | 行高 | 用途 |
| --- | --- | --- | --- | --- |
| Display | `clamp(40px, 5vw, 72px)` | 560 | 1.02 | 页面核心主张 |
| H1 | `36px` | 600 | 1.15 | 一级页面标题 |
| H2 | `20px` | 600 | 1.3 | 模块标题 |
| H3 | `15px` | 600 | 1.4 | 卡片标题 |
| Body | `14px` | 400 | 1.7 | 正文与表单值 |
| Small | `12px` | 400 | 1.6 | 辅助说明 |
| Label | `10px` | 600 | 1.4 | 英文标签、状态；字距 `0.12em` |

- 中文标题避免极粗字重；正文不得小于 12px。
- 数字、请求 ID、模型名和状态使用等宽字体。
- 标题与正文至少形成 1.35 倍字号差，避免所有信息同一音量。

## 4. 间距与留白

采用 4px 基础栅格：`4 / 8 / 12 / 16 / 24 / 32 / 48 / 64`。

- 页面水平边距：桌面 `clamp(24px, 4vw, 64px)`，移动端 `16px`。
- 页面模块间距：`32px`，大区块使用 `48px`。
- 卡片内边距：桌面 `24px`，紧凑卡片 `18px`，移动端 `16px`。
- 表单字段间距：`14px`；标签与输入之间 `8px`。
- 按钮组间距：`8px`。
- 标题与正文间距：`12px`；正文与下一操作区 `20–24px`。
- 单行控件最低高度：`44px`；主操作按钮最低高度：`52px`。

留白用于建立层级，不使用大量边框切割每一行。

## 5. 组件样式

### 按钮

- 主按钮：`#D7FF55` 背景、`#0B0E0A` 文字、圆角 `10px`、无常驻阴影。
- 次按钮：透明或 `#171D25` 背景、`#3B4652` 边框、圆角 `10px`。
- 悬停仅上移 `1px` 或改变边框/背景；按下恢复原位。
- 聚焦环：`0 0 0 3px rgba(215, 255, 85, 0.18)`。
- 禁用状态降低透明度，不使用与可点击状态相同的高亮色。

### 卡片

- 背景 `#11161D`，边框 `1px solid #27303A`，圆角 `16px`。
- 默认阴影：`0 18px 50px rgba(0, 0, 0, 0.22)`。
- Spotlight 只允许使用 `rgba(215, 255, 85, 0.08)`，半径约 `320px`。
- 悬停边框可提升至 `#3B4652`，禁止明显放大和大角度旋转。

### 输入控件

- 背景 `#0C1016`，边框 `#27303A`，圆角 `10px`。
- 内边距 `12px 14px`，正文 13–14px。
- 聚焦边框使用 `#D7FF55`，同时显示低强度聚焦环。
- 错误说明使用 `#FF746C`，不能只依赖颜色表达。

### 导航栏

- 桌面侧栏宽 `84px`；背景 `rgba(9, 11, 15, 0.88)` 并使用轻微模糊。
- 当前项使用 2px 品牌色指示线和低强度品牌色底。
- 图标 20–22px，文字 10px；导航项圆角 `12px`。
- 移动端改为顶部或底部横向导航，不保留挤压内容的固定侧栏。

### 状态与标签

- 使用胶囊形：圆角 `999px`，高度 24–28px。
- 成功用青绿色，警告用暖黄色，错误用珊瑚红。
- 标签只表达状态，不承担主操作。

## 6. 布局

- 桌面采用“窄侧栏 + 顶部状态条 + 主内容区”。
- 创作页先展示目标与预算，再展示四步流程，主工作区为 `5:7` 双栏。
- 左栏承载输入与费用确认；右栏以大画布为核心，下方放质量与模型信息。
- 历史任务使用横向可扫描列表；档案页使用响应式 Bento 网格。
- 1120px 以下压缩双栏；860px 以下改为单栏；560px 以下按钮和数据卡纵向排列。

## 7. 动效

- 页面进入：透明度 `0 → 1`、位移 `8px → 0`，时长 `320–480ms`。
- 列表项允许按 `40ms` 错峰进入，上限 8 项。
- Spotlight 跟随指针，但只改变背景光斑，不改变卡片位置。
- 状态运行中使用低频呼吸，不使用快速闪烁。
- `prefers-reduced-motion: reduce` 时关闭所有非必要动画和过渡。
