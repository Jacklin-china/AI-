# 复制为 .env 后填写（.env 已被 .gitignore 排除：绝不提交、绝不截图外发）
# 各平台控制台申请；泄露处理：立即在平台作废重发，并检查 git 历史是否有残留

# 火山方舟（主力：豆包 Seed 2.1 Turbo 文本 + 视觉；即梦生图也用这个）
ARK_API_KEY=

# 智谱开放平台（降级位 glm-4.6；主力不可用时自动切）
BIGMODEL_API_KEY=
